#!/bin/bash

# https://github.com/KorySchneider/wikit
# wikit empire state building
# wikit linux -b
# wikit jugo --link -a
# -l|--lang langCode   	 Specify language; langCode is an HTML ISO language code
# --all|-a             	 Print all sections of the article (the full page). Recommended to pipe into a reader e.g. less
# --line num           	 Set line wrap length to num
# --link               	 Print a link to the full article after the summary
# -b|--browser browser 	 Open full Wikipedia article in specific browser
# -d                   	 Open disambiguation CLI menu
# -D                   	 Open disambiguation page in browser
# --version|-v         	 Print installed version number
# --name|-n            	 Print the name of the program: wikit

npm install -g wikit
